name: Lint

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  contents: write

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: "pip"

      - name: Install dependencies
        run: pip install ".[dev]"

      - name: Run Ruff format
        run: ruff format .

      - name: Run Ruff check
        run: ruff check . --fix

      # Create symlink with valid Python name for ty validation
      # (4noks_elios4you starts with a digit, which is invalid for Python imports)
      - name: Create symlink for ty
        run: ln -s 4noks_elios4you custom_components/fournoks_elios4you

      - name: Run ty
        run: ty check -vv --python $(which python) custom_components/fournoks_elios4you

      - name: Remove symlink
        run: rm custom_components/fournoks_elios4you

      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: "style: auto-format code"
