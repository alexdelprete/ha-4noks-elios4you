name: Lint

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  contents: write

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"

      - name: Install dependencies
        run: pip install ".[dev]"

      - name: Run Ruff format
        run: ruff format .

      - name: Run Ruff check
        run: ruff check . --fix

      # Create symlink with valid Python name for mypy validation
      # (4noks_elios4you starts with a digit, which is invalid for Python imports)
      - name: Create symlink for mypy
        run: ln -s 4noks_elios4you custom_components/fournoks_elios4you

      - name: Run mypy
        run: mypy custom_components/fournoks_elios4you

      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "style: auto-format code"
